<%= form_with model: [@post, @post.comments.build], local: true, method: :post do |c| %>
  <p>
    <%= c.label :content %><br/>
    <%= c.text_field :content %><br/>
    <%= c.submit "Save changes" %>
  <% end %>
